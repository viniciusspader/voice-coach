# Environment variables — never commit real values
.env

# CDK synthesized output — auto-generated, never commit
cdk.out/

# CDK context (may contain account/region bootstrap info)
cdk.context.json

# Dependencies
node_modules/
